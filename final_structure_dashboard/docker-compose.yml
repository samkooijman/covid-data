version: "3.7"
services:
  app-name:
    build:
      context: .
    image: covid:1.0.0
    container_name: app-name
    ports:
      - "8080:8080"
    environment:
      - TARGET=LIVE
    restart: unless-stopped